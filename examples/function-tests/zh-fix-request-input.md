# 旅行搭子 App 上线前说明

请用 `$vibe-launch-review` 帮我审核这个 vibe coding 做的产品，顺便把明显有问题的地方帮我改掉。我们准备本周末公开上线。

## 产品说明

- 登录只用邮箱验证码，前端按钮可以反复点击 `/api/email-code`。目前没有整理限流、每日预算或异常告警材料。
- 用户可以发布结伴出行动态和留言，动态会公开展示。我们有“用户举报”按钮截图，但没有后台审核队列、下架流程或运营责任文档。
- 用户可以上传头像和行程封面。说明里写了只接受 `jpg/png`，但没有文件大小限制、MIME 校验、对象存储权限、带宽控制或清理策略证据。
- AI 行程生成在 `/api/ai/plan`，提示词保存在服务端，API key 不在前端。当前没有每用户额度、总预算、输出审核或敏感日志说明。
- 管理后台 `/admin` 已经接入登录，但审核包里没有服务端鉴权代码、发布构建配置或 demo 路由移除证据。

## 已提供材料

- 产品上线说明。
- 两张界面截图：举报按钮、上传入口。
- 没有提供后端路由代码、存储配置、AI 额度配置、后台鉴权代码或发布构建配置。

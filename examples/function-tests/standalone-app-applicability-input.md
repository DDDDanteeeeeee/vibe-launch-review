# 独立桌面应用上线前验收请求

请使用 `$vibe-launch-review` 给这个项目做一份中文交付验收和上线决策报告，读者是老板、产品和销售，不都是技术背景。

项目情况：

- 产品是一个 Windows 桌面应用，用户安装后在本地选择 Illustrator `.ai` 文件。
- 用户通过授权码激活设备，再通过点数码获得使用点数。
- 应用没有短信验证码、邮件验证码、魔法链接、通知发送或邀请邮件。
- 应用没有社区、评论、公开资料页、用户帖子、用户昵称墙、作品广场或公开分享页。
- 用户选择的 `.ai` 文件本体不上传到云端。客户端只提取需要翻译的文本片段并发送给云端。
- 云端调用 AI 模型完成翻译，并按任务扣点。模型 key 不放在客户端。
- 已有 smoke 证据显示：真实模型翻译通过，任务成功后扣点；模型调用失败时按 `taskId` 退款。
- 还缺少公开发布前证据：模型调用接口的 per-device/per-license 限流、全局预算上限、异常监控、生产包 endpoint 校验、以及本地任务目录清理策略。

请先判断项目类型和五门适用性。不要因为没有社区功能就强行写 UGC 风险。只找出问题、说明风险、给出审查建议，不直接修改代码。
